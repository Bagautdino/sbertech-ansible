---
- name: ADD USER TO HOSTS
  hosts: servers
  become: true
  become_method: ansible.builtin.sudo
  vars_files:
    - vault.yml
#  vars:
#    ansible_become_password: "{{ ansible_become_pass }}"
  roles:
    - user
